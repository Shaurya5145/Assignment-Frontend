{
  "buildCommand": "npx vite build --config vite.config.frontend.ts",
  "outputDirectory": "dist",
  "devCommand": "npx vite --config vite.config.frontend.ts",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}